spring:
  application:
    name: JobPortalWebsite

  datasource:
    url: jdbc:mysql://localhost:3306/job_portal
    username: root
    password: root

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update

server:
  port: 8080
  servlet:
    context-path: /JobPortalWebsite
